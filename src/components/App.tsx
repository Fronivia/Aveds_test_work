import React from 'react';
import styles from './app.module.scss';

const App = () => {
    return (
        <div className={styles.appTest}>
            Hello World
        </div>
    );
};

export default App;
